# 双人合作模式网络设计

## 架构
采用状态同步：主机运行完整游戏逻辑，每帧发送游戏状态（球、板、砖块、道具数据）；客户端接收状态并绘制，同时将自己的板位置发送给主机。

## 协议
使用 ENet (UDP) 传输，消息结构包括 GameStateMessage 和 PaddleUpdateMessage，使用固定数组存储动态数据（最多8球、20道具、40砖块）。

## 平滑处理
客户端直接使用最新状态绘制，不插值（网络延迟低，体验可接受）。