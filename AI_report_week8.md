# 第八周 AI 对话记录

## 提问
在 C++ 中如何实现异步加载资源，而不让游戏卡顿？

## AI 回答
使用 std::async 创建后台任务，主线程通过 future.wait_for 非阻塞检查完成状态；用互斥锁保护共享状态如加载标志；加载期间主线程继续游戏循环。

## 我的审查
采纳了建议，实现了按 L 键触发异步加载，使用 std::launch::async 启动任务，用 std::mutex 保护 loadState 和 loadCompleted，加载完成后改变砖块颜色。演示效果符合要求，没有出现数据竞争或死锁。